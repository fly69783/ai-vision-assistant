# frontend

当前前端是一个不需要Node.js的原生网页测试台，文件位于`web/`。

启动后端后访问 <http://127.0.0.1:8000> 即可使用。页面负责：

- 选择图片和任务。
- 提交到`/api/v1/analyze`。
- 展示画面质量、能力状态、警告和结构化结果。
- 使用浏览器自带语音朗读后端返回的播报文案。

前端不会生成模拟识别结果。后端没有接入模型时，页面会明确显示“真实AI能力尚未配置”。
